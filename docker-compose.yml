version: '3.3'

services:
  letsencrypt: webitdesign/letsencrypt-cron
    container_name: letsencrypt
    volumes:
      - ./certs:/certs
      - ./cert-config.yml:/le/certs.yml
    ports:
      - '80:80'
    restart: always